# 저장소 에이전트 지침

이 지침은 이 저장소에서 작업하는 모든 자동화 코딩 에이전트에 적용한다.

## 언어

- README, 협업 문서, 결정 기록, PR 본문 등 사람이 읽는 설명은 한국어를 기본으로 작성한다.
- 브랜치명, 커밋 메시지, PR 제목은 간결한 영어로 작성한다.
- 코드 식별자, 명령어, 파일명, 표준 기술 용어는 정확성을 위해 필요한 경우 영어를 유지한다.

## PR 작업 흐름

- 일반적인 작업 흐름에서는 추적 파일 변경을 `main`에 직접 커밋, 푸시 또는 병합하지 않는다.
- 수명이 짧은 브랜치를 만들고 PR로 변경사항을 전달한다.
- 저장소 PR 템플릿을 사용하며 최상위 구역을 추가하지 않는다. `변경사항`과 `검증`만 유지한다.
- PR 작업 흐름을 생략하려는 상황에서는 이 저장소가 PR을 요구한다는 점을 사용자에게 명확히 알린다.
- 사용자가 `main` 직접 커밋, 푸시 또는 병합을 요청하면 저장소 규칙을 우회하는 작업임을 설명하고 명시적인 확인을 받은 뒤 진행한다.
- 읽기 전용 조사, 계획 수립 또는 추적 파일 변경이 없는 답변에는 PR이 필요하지 않다.
- GitHub URL을 확인하기 전에는 PR이 생성되었다고 말하지 않는다.

## 변경 원칙

- 브랜치와 PR 하나에는 한 가지 목적만 담는다.
- 사용자에게 속한 관련 없는 변경은 수정하지 않는다.
- 설정, 동작, 계약 또는 운영 가정이 바뀌면 README나 결정 기록도 함께 갱신한다.
- 실행한 명령과 결과를 PR의 `검증`에 기록하고, 관련 검증을 실행하지 못했다면 그 사실을 명시한다.
- 변경을 통과시키기 위한 목적으로 테스트 기대값을 약화하거나 삭제하거나 재생성하지 않는다.

## 모바일·백엔드 연동

- 백엔드 OpenAPI 명세를 API 계약의 기준으로 삼는다.
- 모바일에 영향을 주는 계약 변경은 백엔드 저장소와 함께 조율하고 관련 PR을 연결한다.
- 직접 중복 작성한 DTO보다 생성된 API 타입을 우선한다.
- 가능한 한 하위 호환성을 유지한다. 호환성을 깨는 변경은 대체 계약 추가, 사용처 이전, 기존 계약 제거 순서로 진행한다.

## 데이터와 보안

- 소스, 테스트 데이터, 로그, 스크린샷, 예시에는 합성 데이터만 사용한다.
- 자격 증명, 토큰, 로컬 환경 파일, 계정 식별자, 실제 거래 데이터, 구체적인 임신 정보를 커밋하지 않는다.
- `EXPO_PUBLIC_` 변수는 클라이언트 번들에 포함되므로 비밀값을 넣지 않는다.
- 원본 금융 데이터나 임신 정보를 외부 LLM으로 보내지 않는다.
